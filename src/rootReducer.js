let state = {
    cities: []
};

function updateStorage(state, action) {
    if (action === 'ADD_CITY')
        return state;
    else if (action === 'REMOVE_CITY')
        return state;
    else
        return state;
};

